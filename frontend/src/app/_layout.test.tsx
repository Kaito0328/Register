// 基本的なテストスイート
describe('App Layout', () => {
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should have Jest working', () => {
    expect(true).toBeTruthy();
  });
});
